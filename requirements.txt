# ADOFAI Diffusion —— 运行 / 训练依赖
# 安装：pip install -r requirements.txt
# 推理与训练均需以下包；GPU 版 torch 请按你的 CUDA 版本从
# https://pytorch.org 选取对应命令（本项目便携包使用 torch 2.11 + cu128）。

torch>=2.0
numpy
matplotlib
librosa
demucs
soundfile
scipy
